git config --global user.name "John Doe"
git config --global user.email johndoe@example.com
git init
git remote add [origin,dev,master]

--chequeo
git fetch && git checkout master

--bajar repositorio
git pull

--subir cambaios
cat .gitignore

git add --all

git commit -m "Subir cambios - texto"

git push -u -f origin master 
